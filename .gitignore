# Build output
bin/
obj/
AppxPackages/
AppPackages/
/*.txt
/*.ps1

# Auto-generated MSIX assets (recreated by scripts/create-assets.ps1 on first build)
# Anchored to windows/Assets specifically — an unanchored "Assets/" also matched the
# root-level assets/ (source logo.svg, tracked, shared with mobile/ and README.md).
/windows/Assets/

# Signing certificate — never commit
windows/scripts/Hatch-signing.pfx
windows/scripts/hatch-signing.pfx
*.pfx
signing.pfx

# Visual Studio
.vs/
*.user
*.suo

# Rider
.idea/

# Secrets — never commit; generated by CI or created manually for local dev
windows/Services/Secrets.cs

# Kotlin Multiplatform companion clients (mobile/) — Gradle build output
mobile/.gradle/
mobile/.kotlin/
mobile/build/
mobile/*/build/
mobile/local.properties
